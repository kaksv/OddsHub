# Local only (npm run dev). Never set VITE_API_PROXY on Vercel production.
VITE_API_PROXY=http://127.0.0.1:8787

# Reown AppKit — https://dashboard.reown.com
# For Vercel: add VITE_REOWN_PROJECT_ID under Project → Settings → Environment Variables (Production).
# Allow origin https://odds-hub.vercel.app in the Reown dashboard.
VITE_REOWN_PROJECT_ID=
