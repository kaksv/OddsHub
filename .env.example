# Local API proxy (npm run dev)
VITE_API_PROXY=http://127.0.0.1:8787

# Reown AppKit — free project ID from https://dashboard.reown.com
# Required for wallet connect. Do NOT use localhost project restrictions in prod without updating allowed origins.
VITE_REOWN_PROJECT_ID=
